Automatically deploy tailscale exit node to Vultr VPS
$5.00 / month VPN with tailscale. 
Also runs pihole DNS adblocker which can be configured as tailnet DNS server for secure private DNS and adblocking on all tailnet clients
